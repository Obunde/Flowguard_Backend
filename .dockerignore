.venv/
.git/
.env
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
.python-version
# NOTE: README.md is NOT ignored — pyproject.toml's `readme =` field makes
# hatchling require it to exist at build time (uv sync builds the project).
